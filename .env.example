# Neon PostgreSQL — development branch
DATABASE_URL="postgresql://user:password@host/dbname?sslmode=require"

# Neon PostgreSQL — production branch (used by prisma migrate deploy)
# DATABASE_URL_PROD="postgresql://user:password@host/dbname?sslmode=require"

# NextAuth
AUTH_SECRET=""
AUTH_GITHUB_ID=""
AUTH_GITHUB_SECRET=""
